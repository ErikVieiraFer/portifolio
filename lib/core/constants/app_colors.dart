import 'package:flutter/material.dart';

class AppColors {
  static const primaryPink = Colors.pinkAccent;
  static const transparentBlack = Color.fromRGBO(0, 0, 0, 0.4);
  static const white70 = Colors.white70;
}
